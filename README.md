# arch-linux-bootstrap-scripts

My scripts for bootstrapping an Arch Linux installation.

`install.sh` contains scripts meant to be run in the live disk.

`post-install.sh` contains scripts meant to be run after rebooting into the fresh install.
